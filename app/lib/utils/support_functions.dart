import 'package:flutter/material.dart';

class SupportFunctions extends StatelessWidget {
  const SupportFunctions({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
} 